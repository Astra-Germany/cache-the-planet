# Java-Cache-Beispiel

`actions/setup-java` wird nur zur Installation des JDK verwendet. Der integrierte `cache`-Input bleibt deaktiviert. Das Maven Repository wird mit `-Dmaven.repo.local` nach `.cache/m2` umgeleitet und über die Content-Addressed-Asset-Action gespeichert.
