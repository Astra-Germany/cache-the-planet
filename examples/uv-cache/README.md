# uv-Cache-Beispiel

Der uv-Cache wird mit `cache-local-path: .cache/uv` in den Workspace umgeleitet. `enable-cache` bleibt deaktiviert, damit `astral-sh/setup-uv` keinen nativen GitHub-Actions-Cache verwendet.
